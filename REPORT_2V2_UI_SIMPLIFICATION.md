# تقرير إصلاح واجهة 2v2 Team Battle

## القرار التنفيذي

تم تنفيذ التعديل المطلوب على شاشة اللعب في **2v2 Team Battle**. الحالة الحالية هي **Conditional Ready**: التعديل موجود في المشروع، والتحقق الساكن للعقود الأساسية نجح، لكن يجب إعادة تشغيل اختبار البناء والتشغيل المحلي بعد إغلاق أي عملية Node قديمة من بيئة Windows قبل اعتماد التعديل كـ **Ready** نهائيًا.

## ما تم تغييره

أصبحت الشاشة تعرض بطاقة صغيرة للفريق الحالي، وتحتوي على أسماء اللاعبين الفعليين الذين دخلوا الغرفة، مثل أحمد ومحمد أو يوسف ويوسف، مع اسم الفريق `TEAM A` أو `TEAM B`. لم يعد هناك بلوك منفصل بعنوان `YOU ARE TEAM B` أو عرض متكرر لأسماء اللاعبين داخل واجهة اللعب.

يظهر الآن هدف الفريق المنافس بصورته واسمه فقط. فإذا كان اللاعب في `TEAM A`، تظهر بطاقة `TEAM B TARGET`، وإذا كان في `TEAM B`، تظهر بطاقة `TEAM A TARGET`. مصدر البطاقة هو `actions.privateTarget` المرتبط بالاشتراك الخاص باللاعب، ولذلك لا يتم عرض هدف الفريق الذي ينتمي إليه اللاعب نفسه.

تم حذف مؤقت `TIME LEFT` من شاشة Team Battle، كما تم حذف `GUESS BOARD` وجميع بطاقات الصور الخمس عشرة. لم يتم حذف مكوّن `GuessGrid` من المشروع بالكامل؛ تمت إعادته كمكوّن مشترك تحتاجه شاشة Tournament، لكنه لم يعد مستخدمًا داخل `TeamBattleGameplay`. هذا يمنع حدوث Regression في Tournament.

تم استبدال النصوص الطويلة بزر واحد واضح مثل `TEAM B GUESSED CORRECT` أو `TEAM A GUESSED CORRECT`. الزر ما زال يستدعي `actions.confirmTeamGuess()` نفسه، ويظل مقفولًا عندما لا يكون اللاعب ضمن الفريق المطلوب للتأكيد، أو عندما يكون قد أكد بالفعل، أو عندما تكون الحالة غير قابلة للتعديل.

## الضمانات التي لم تتغير

| المجال | الحالة بعد التعديل |
|---|---|
| خصوصية الهدف | الفريق يرى هدف الفريق المنافس فقط؛ هدف فريقه لا يُعرض في شاشة اللعب. |
| التأكيد الجماعي | لا ينتقل الدور إلا بعد تأكيد لاعبي الفريقين المطلوبين حسب الحالة السلطوية. |
| Firebase | لم يتم تغيير عمليات الاشتراك الخاصة أو معاملات الغرفة أو مصدر الحالة السلطوية. |
| انتقال الجولة | بقي مسار `requiredTeamsComplete` ثم `actions.resolveTeamRound()` قائمًا. |
| Tournament | تمت المحافظة على `GuessGrid` كمكوّن مستخدم في Tournament، وعدم حذف المكوّن المشترك. |
| 1v1 | لم يتم تعديل مكوّنات 1v1 أو مساراتها. |
| الهاتف | بقيت أزرار التأكيد كبيرة، مع `min-h-14` و`focus-visible:ring-2`، للحفاظ على سهولة اللمس وإمكانية الوصول. |

## التحقق المنفذ

تم تنفيذ تحقق ساكن مباشر على الملف المعدل، وكانت النتائج كما يلي:

| الفحص | النتيجة |
|---|---|
| وجود `TeamBattleIdentity` | ناجح |
| وجود تسمية الهدف المنافس الديناميكية | ناجح |
| عرض صورة الهدف المنافس | ناجح |
| غياب `GuessGrid` من `TeamBattleGameplay` | ناجح |
| غياب `TIME LEFT` و`roundEndTimestamp` من شاشة Team Battle | ناجح |
| بقاء `confirmTeamGuess()` | ناجح |
| بقاء مسار التأكيدين `requiredTeamsComplete` و`resolveTeamRound()` | ناجح |
| بقاء `GuessGrid` في المشروع لخدمة Tournament | ناجح |
| غياب `OPPONENT TARGET · HIDDEN` من شاشة Team Battle | ناجح |

كانت هناك مشكلة تنفيذية أثناء التحقق: كانت عمليات Git وNode القديمة تمسك ملف `CompetitiveModePage.jsx` في Windows عبر memory-mapped section، فتم إيقاف عمليات التطوير المرتبطة بالمشروع وإغلاق عمليات Git القديمة. بعد ذلك تم تثبيت التعديل وإجراء تحقق ساكن جديد. لم يتم تسجيل Crash أو تعديل في Firebase adapter أثناء هذه العملية.

## ملاحظة مهمة عن منطق اللعبة

هذا التعديل يطبق القاعدة التي حددتها: **Team A يرى هدف Team B، وTeam B يرى هدف Team A**. معنى ذلك أن بطاقة الهدف الظاهرة ليست هدف اللاعب نفسه، بل الهدف الذي يحاول الفريق المنافس تخمينه. أما زر `GUESSED CORRECT` فيمثل موافقة لاعبي الفريق الذي يملك الهدف، ولذلك بقيت صلاحية الضغط مرتبطة بالفريق المطلوب وبالتأكيدين المنفصلين في Firebase.

## الخطوة الأخيرة المطلوبة

قبل نقل الحالة إلى **READY** النهائي، يجب تشغيل `npm run build` واختبارات `qa-team-battle-ui.mjs` و`qa-team-battle-engine.mjs` و`qa-smoke.mjs` بعد توقف عمليات Node القديمة بالكامل، ثم فتح `/team-battle` محليًا وفحص شاشة اللعب على الهاتف. كما يجب إجراء اختبار staging بأربعة عملاء مستقلين للتأكد من أن فريق A يرى هدف فريق B والعكس، وأن التأكيد الأول لا ينقل الجولة قبل التأكيد الثاني.

**الخلاصة:** تم تنفيذ تصميم الشاشة المطلوب، وتم الحفاظ على منطق Firebase والتأكيد الجماعي، وتمت معالجة خطر Regression الخاص بـ Tournament بإعادة `GuessGrid` كمكوّن مشترك. الاعتماد النهائي مشروط بإعادة تشغيل build/runtime بعد تحرير بيئة Windows من العمليات القديمة.
