# تقرير إصلاح 2v2 Team Battle

## ملخص المشكلة

ظهرت في شاشة اللعب حالتان تؤثران على الاستخدام. زر `Leave`، الذي يظهر للمستخدم كزر Disconnect/مغادرة، كان يدخل في حالة التعطيل عند وجود أي `pendingAction` آخر، ولذلك يظهر باهتًا ولا يستجيب للنقر حتى لو كان الإجراء المعلّق لا علاقة له بالمغادرة. كما كانت بطاقة الفريق تعرض هوية اللاعبين بصورة مربكة، إذ تظهر أرقام أو مؤشرات لا تعبر عن الاسم الذي سجله اللاعب داخل الغرفة، وتعرض `YOUR TEAM` بدل اسم اللاعب الحالي.

## الإصلاح المنفذ

تم حصر تعطيل زر المغادرة في حالة واحدة فقط: `pendingAction === 'leave'`. وبذلك لا يؤدي تأكيد الجولة أو تغيير الفريق أو إجراء آخر إلى تعطيل زر Disconnect. ما زال الإجراء يمر عبر دالة `run` الحالية، لذلك تبقى معالجة رفض Firebase ورسائل الخطأ كما هي، ولا توجد كتابة جديدة أو تجاوز لقواعد الغرفة.

تم استبدال بطاقة الهوية المنفردة ببطاقة `TEAM ROSTERS` تعرض `TEAM A` و`TEAM B` مع أسماء اللاعبين الموجودة في `state.players`، وهي الأسماء المسجلة في الغرفة. يحصل الفريق الحالي على شارة `YOUR NAME`، ويظهر اسم اللاعب الحالي أسفل فريقه، كما يتم تمييز اسمه بصريًا دون تغيير معرّف اللاعب أو توزيع الفرق.

## حدود الحماية

لم يتم تعديل محرك الجولات، اختيار الأهداف، خصوصية الأهداف، التأكيد الثنائي، التسجيل، الانتقال بين الجولات، Firebase adapter، أو قواعد Realtime Database. التعديل محصور في عرض الهوية وحالة زر المغادرة. بقيت أزرار تبديل الفريق وقواعد الامتلاء وبداية المباراة مرتبطة بالإجراءات authoritative الموجودة أصلًا.

| المجال | النتيجة |
|---|---|
| زر Disconnect/Leave | لا يتعطل بسبب إجراءات غير مرتبطة؛ يتعطل فقط أثناء المغادرة نفسها |
| أسماء اللاعبين | مأخوذة من `state.players[id].name` |
| الفريق الحالي | يظهر بشارة `YOUR NAME` واسم اللاعب الحالي |
| Team A / Team B | يعرضان أسماء المسجلين في الغرفة |
| منطق اللعب | لم يتغير |
| Firebase والقواعد | لم تتغير |
| 1v1 وTournament | لا تعتمد على مكوّن الهوية الجديد |

## التحقق

نجح عقد واجهة Team Battle بعد إضافة assertions خاصة بوجود `TEAM ROSTERS` و`YOUR NAME`، وتحقق عدم وجود اعتماد غير محلول على `TEAM_IDS.A/B`، وتحقق أن `disabled` الخاص بالمغادرة مربوط بـ `pendingAction === 'leave'` فقط. كما نجحت اختبارات smoke، ونجح بناء Vite الإنتاجي باستخدام الملف التنفيذي المحلي، وكانت النتائج `UI=0`, `SMOKE=0`, و`BUILD=0`.

اختبار Firebase staging بأربعة عملاء authenticated ما زال خطوة ما قبل الإنتاج، لأن البيئة المحلية لا تثبت وحدها صحة الجلسات الحقيقية وقواعد Firebase المنشورة. هذا القيد لا يمنع اعتماد الإصلاح محليًا، لكنه يمنع وصفه بأنه جاهز إنتاجيًا دون اختبار staging.

## القرار

الحالة الحالية: **FIXED LOCALLY / CONDITIONAL READY**. الإصلاح يعالج تعطّل زر Disconnect والهوية المربكة دون مساس بقوانين اللعب أو منطق الجولات. الخطوة المتبقية هي اختبار غرفة 2v2 فعلية بأربعة لاعبين، التأكد من ظهور الأسماء على الأجهزة الأربعة، وتجربة المغادرة أثناء lobby وأثناء الجولة وأثناء وجود عملية تأكيد جارية.

_إعداد: Manus AI_ 
        
---

## مراجع داخلية

- `src/pages/CompetitiveModePage.jsx`
- `scripts/qa-team-battle-ui.mjs`
- `scripts/qa-smoke.mjs`
- `src/modes/teamBattleEngine.js`
- `src/firebase/competitiveFirebase.js`
- `database.rules.json`
