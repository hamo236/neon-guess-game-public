const port=9332;const page=(await fetch(`http://127.0.0.1:${port}/json/list`).then(r=>r.json())).find(t=>t.type==='page');const ws=new WebSocket(page.webSocketDebuggerUrl);await new Promise((res,rej)=>{ws.onopen=res;ws.onerror=rej});let id=0;const pending=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data),p=pending.get(m.id);if(p){pending.delete(m.id);p(m)}};const send=(method,params={})=>new Promise(res=>{const i=++id;pending.set(i,res);ws.send(JSON.stringify({id:i,method,params}))});
const expr="(()=>{const input=document.querySelector('input[placeholder=\"Your name\"]');const s=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set;s.call(input,'HostOne');input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}));[...document.querySelectorAll('button')].find(b=>b.innerText.includes('3-4 (Impostor)'))?.click();[...document.querySelectorAll('button')].find(b=>b.innerText.includes('Create Room')&&b.innerText.includes('add_circle'))?.click();return 'submitted'})()`;await send('Runtime.evaluate',{expression:expr,returnByValue:true});await new Promise(r=>setTimeout(r,2200));const out=await send('Runtime.evaluate',{expression:"({url:location.href,text:document.body.innerText,storage:Object.fromEntries(Object.entries(sessionStorage))})",returnByValue:true});ws.close();await import('node:fs/promises').then(fs=>fs.writeFile('qa-fresh-room.json',JSON.stringify(out.result?.result?.value,null,2)));
