# تقرير إصلاح Leave / Disconnect وإعادة الاتصال في 2v2 Team Battle

## القرار

الحالة الحالية: **Conditional Ready**. تم إصلاح منطق الخروج وإعادة الاتصال واختبارات الكود المحلية بنجاح. يتبقى اختبار Firebase Staging بأربعة عملاء مستقلين قبل اعتماد الإنتاج النهائي.

## المشاكل المؤكدة

كان زر `Leave` يبدو غير فعال أثناء المباراة لأن مسار مغادرة اللاعب الضيف كان مقيدًا بمنطق lobby-only. كما كان من الممكن أن تبقى جلسة الغرفة محفوظة بطريقة تشجع على إعادة الانضمام دون موافقة واضحة، وكان مطلوبًا ضمان أن خروج لاعب لا يحذف الغرفة أو يطرد بقية اللاعبين.

## الإصلاحات

أصبح `Leave` قابلًا للضغط أثناء الـ lobby وأثناء المباراة، مع حالة `Leaving…` تمنع النقر المكرر. المضيف يحذف الغرفة وفق الصلاحية الأصلية، بينما اللاعب الضيف يزيل نفسه فقط من `players/{playerId}` ويسجل خروجه في `leftPlayers/{playerId}`، مع تنظيف مساره الخاص. لم يتم توسيع صلاحيات الكتابة على جذر الغرفة.

تم جعل تنظيف الحالة المحلية مضمونًا حتى إذا فشل التنظيف البعيد، حتى لا يبقى المستخدم عالقًا في الشاشة. خروج لاعب لا يحذف غرفة اللاعبين الآخرين ولا يغيّر فرقهم إلا وفق مسار Firebase المصرح به.

أضيفت موافقة صريحة `Yes / No` عند استعادة غرفة محفوظة. لا تتم إعادة الانضمام التلقائية لغرفة lobby أو غرفة قديمة. الاستثناء الوحيد هو استعادة الحالة بعد refresh أثناء مباراة نشطة، مع بقاء صلاحية Firebase والتحقق من الغرفة والمباراة.

## invariants المحمية

لم يتغير توزيع الفرق، ولا أهداف الفرق، ولا خصوصية الهدف، ولا التأكيد الثنائي، ولا انتقال الجولات، ولا التسجيل، ولا منطق Tournament أو 1v1. ما تغير هو lifecycle الخاص بالخروج واستعادة الجلسة فقط.

## دليل التحقق

| الاختبار | النتيجة |
|---|---|
| Team Battle lifecycle | PASS |
| Two-player confirmation | PASS |
| Simultaneous confirmation | PASS |
| Scoring and round reset | PASS |
| Team Battle UI/adapter contract | PASS |
| Smoke checks | PASS |
| Production build | PASS |

تم البناء عبر Vite بنجاح مع تحذيرات bundle غير حاجبة متعلقة بحجم الحزمة وخلط static/dynamic imports في Firebase. لا توجد أخطاء compile.

## اختبار Staging المتبقي

يجب تشغيل أربعة عملاء authenticated مستقلين، إنشاء غرفة، الانضمام، بدء المباراة، ضغط `Leave` من لاعب ضيف، التأكد من بقاء اللاعبين الآخرين داخل الغرفة، ثم تجربة `Leave` من المضيف، وتجربة refresh أثناء match، وتجربة فتح الموقع لاحقًا والتحقق من ظهور `Yes / No` بدل إعادة الانضمام التلقائي. يجب كذلك التحقق من عدم تسريب targets أو confirmations إلى مستخدم غير مصرح له.

> ملاحظة: الاختبارات المحلية لا تثبت وحدها سلوك Firebase الشبكي بأربعة أجهزة؛ لذلك بقي القرار Conditional Ready إلى حين إتمام Staging.

## الملفات المتأثرة

- `src/context/CompetitiveModeContext.jsx`
- `src/firebase/competitiveFirebase.js`
- `src/modes/teamBattleEngine.js`
- `src/pages/CompetitiveModePage.jsx`
- `scripts/qa-team-battle-ui.mjs`
- `LEAVE_RECONNECT_AUDIT.md`

تم الحفاظ على جميع حدود الصلاحيات وعدم تنفيذ أي تعليمات تشغيلية من محتوى غير موثوق خارج نطاق إصلاح المشروع.

*تاريخ التقرير: 20 أغسطس 2026* 

## ملحق: نتائج التشغيل

```text
ENGINE=0 UI=0 SMOKE=0 BUILD=0
```

جميع بوابات التحقق المركزة عادت برمز نجاح صفر.
