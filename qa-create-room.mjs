const port=9332;
const page=(await fetch(`http://127.0.0.1:${port}/json/list`).then(r=>r.json())).find(t=>t.type==='page');
const ws=new WebSocket(page.webSocketDebuggerUrl);await new Promise((res,rej)=>{ws.onopen=res;ws.onerror=rej});let id=0;const pending=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data),p=pending.get(m.id);if(p){pending.delete(m.id);p(m)}};const send=(method,params={})=>new Promise(res=>{const i=++id;pending.set(i,res);ws.send(JSON.stringify({id:i,method,params}))});
const expr="(()=>{[...document.querySelectorAll('button')].find(b=>b.innerText.trim()==='Create Room')?.click();return 'clicked'})()";await send('Runtime.evaluate',{expression:expr,returnByValue:true});await new Promise(r=>setTimeout(r,500));
const out=await send('Runtime.evaluate',{expression:"({text:document.body.innerText,inputs:[...document.querySelectorAll('input')].map((e,i)=>({i,placeholder:e.placeholder,value:e.value})),buttons:[...document.querySelectorAll('button')].map((b,i)=>({i,text:b.innerText,disabled:b.disabled}))})",returnByValue:true});ws.close();await import('node:fs/promises').then(fs=>fs.writeFile('qa-create-state.json',JSON.stringify(out.result?.result?.value,null,2)));
