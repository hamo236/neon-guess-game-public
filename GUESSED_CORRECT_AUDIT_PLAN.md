# تقرير تحقيق وخطة إصلاح زر GUESSED CORRECT

## النتيجة المؤكدة

زر `GUESSED CORRECT` كان يظهر بصريًا كأنه معطّل لأن شرط `disabled` في `TeamBattleGameplay` يتطلب `myTeamRequired`. هذا الشرط كان يعتمد على وجود `confirmationTeamId` أو تأكيد محفوظ مسبقًا. بعد إزالة `Guess Board` لم يعد هناك مسار أولي يكتب `confirmationTeamId` قبل الضغط على الزر، لذلك ظل الشرط false، وأصبح الزر غير قابل للنقر حتى مع اتصال Firebase سليم.

## حدود الإصلاح

سيتم تعديل مسارين فقط. أولًا، ستصبح واجهة الزر قابلة للتفعيل لأي لاعب من فريق مكتمل 2v2 أثناء حالة `playing`، ما دام الاتصال يسمح بالكتابة ولم يؤكد اللاعب نفسه الجولة. ثانيًا، سيقبل محرك الجولة تأكيد الفريق الحالي مباشرة، بما يسمح بتأكيدين متزامنين من الفريقين عند الحاجة، مع بقاء التحقق من اللاعب والجولة والمباراة، وبقاء بوابة اللاعبَين لكل فريق، وبقاء الحل authoritative من المضيف.

لن يتم تعديل خصوصية الأهداف، توزيع الفرق، كتابة Firebase لكل لاعب، قواعد الأمان، التسجيل، عدد الجولات، أو مسارات 1v1 وTournament.

## معايير القبول

| الحالة | النتيجة المطلوبة |
|---|---|
| لاعب صالح في جولة playing قبل أي تأكيد | الزر enabled وقابل للنقر |
| اللاعب نفسه أكد الجولة | الزر disabled مع حالة CONFIRMED |
| لاعب الفريق الثاني يضغط بعد تأكيد الفريق الأول | يستطيع التأكيد إذا كانت الجولة تتطلب تأكيدًا متزامنًا |
| لاعب واحد فقط أكد | لا يحدث انتقال للجولة |
| اللاعب الثاني من الفريق نفسه أكد | يمكن للمضيف حل الجولة فقط بعد اكتمال بوابة الفريق |
| اتصال غير جاهز | الزر يبقى disabled لأسباب صحيحة ويظهر وضع الاتصال |
| 1v1 أو Tournament | لا يتغير السلوك |

## خطة التحقق

تشغيل عقد محرك 2v2، عقد واجهة/adapter، smoke، production build، ثم فتح المسار المحلي وفحص console. الاختبار الواقعي بأربعة عملاء Firebase Staging يبقى مطلوبًا لاعتماد الإنتاج النهائي.
